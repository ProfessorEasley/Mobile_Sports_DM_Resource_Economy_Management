# Backend

This directory contains the backend API source code, models, services, and tests for the Resource & Economy Management System.

- `app/`: Main application code (API, models, services, DB).
- `tests/`: Integration and unit tests.
- `Dockerfile`: Containerization for deployment.

See `../readme_harsh.md` for backend responsibilities and workflow.
